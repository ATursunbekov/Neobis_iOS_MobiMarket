import Foundation

struct Constants {
    static let scheme = "https"
    static let baseURL = "mobi-market.up.railway.app" // Only host
    static let port: Int? = nil // Port is not needed if standard (80 for http, 443 for https)
    static let fullURL = "https://mobi-market.up.railway.app/api" // Full URL for reference
}
